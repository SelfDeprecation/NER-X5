# 📘 NER-модель для извлечения сущностей из запросов пользователей мобильного приложения магазина "Пятёрочка"

## 📖 История проекта

Проект начинался с задачи создать устойчивую систему **распознавания именованных сущностей (NER)** в текстах товарных описаний в рамках решения кейса от X5 на хакатоне от правительства Москвы "Лидеры цифровой трансформации".  
Первая версия использовала модель от DeepPavlov (`rubert-base-cased`), но долго не удавалось преодолеть потолок **Macro F1 ≈ 0.81** — модель просто не справлялась с тонкими различиями между типами сущностей.

После нескольких итераций и анализов было принято два ключевых решения:
1. **Добавить предобработку данных**, удалив BIO-префиксы и индексы из разметки (`B-`, `I-`, `(0,5)` → `TYPE TYPE`), чтобы фокусироваться именно на типах сущностей.
2. **Перейти на более сильную модель** — `AI-Forever/RuBERT-base` (в проекте обозначалась как `rubert-base`).

Эти изменения дали серьёзный прирост точности — модель начала стабильно выходить на **Macro F1 ≈ 0.94–0.95** после тонкой настройки.

---

## ⚙️ Структура проекта

```
api/
 ├── helpers.py       # Вспомогательные функции и классы
 ├── schemas.py       # Ptdantic модели для валидации данных
 ├── service.py       # Ендпоинты и хендлеры
src/
 ├── preprocess.py    # Подготовка и конвертация данных в формат token/label
 ├── train.py         # Обучение модели
 ├── predict.py       # Предсказания и генерация submission-файлов
data/
 ├── train.csv        # Обучающая выборка
 ├── submission.csv   # Тестовая выборка
outputs/
 └── ...              # Сохранённые веса и токенайзер


## 🧰 Технологический стек проекта

---

## ⚙️ Используемые технологии

### 🧩 Язык и среда
- **Python 3.9+**
- **Google Colab / Jupyter Notebook / локальная среда**
- **CUDA / GPU T4 (при обучении на Colab)**

---

### 🤖 Основные библиотеки и фреймворки

| Компонент | Библиотека | Назначение |
|------------|-------------|-------------|
| Трансформеры | `transformers` (Hugging Face) | Архитектура модели, токенизация, обучение |
| Датасеты | `datasets` | Работа с наборами данных, токенизация и split |
| Метрики | `seqeval` | Подсчёт метрик F1, precision, recall для NER |
| Оптимизация | `torch` (PyTorch) | Базовая фреймворк для обучения нейросетей |
| Кросс-валидация | `scikit-learn` | Train/Test split, дополнительные метрики |
| Обработка данных | `pandas`, `ast`, `re` | Работа с CSV, разбор аннотаций, регулярные выражения |

---

### 🧱 Используемые модели

| Модель | Источник | Назначение |
|---------|-----------|-------------|
| `DeepPavlov/rubert-base-cased` | Hugging Face Hub | Первая версия модели, F1 ≈ 0.81 |
| `AI-Forever/RuBERT-base` | Hugging Face Hub | Основная модель, F1 ≈ 0.95 |
| (экспериментально) `xlm-roberta-large` | Hugging Face Hub | Тестирование мульти-языкового подхода |

---

### 🧰 Инструменты и утилиты

- **Git / GitHub** — контроль версий и совместная работа  
- **Python argparse** — CLI-параметры для запуска `train.py` и `predict.py`  
- **HuggingFace Trainer API** — управление циклами обучения, логами, сохранением моделей  
- **Weights & Biases (опционально)** — логирование экспериментов и визуализация метрик  
- **VS Code / PyCharm** — основная IDE для разработки  

---

## ⚡ Особенности реализации

- Используется **FP16 mixed precision training** для ускорения на GPU.  
- Модель сохраняет **топ-5 лучших чекпоинтов** по метрике Macro F1.  
- Поддерживается **дообучение лучшей модели** с меньшим learning rate.  
- После обучения автоматически выполняется инференс на тестовом наборе.  
- Весь пайплайн полностью совместим с **Hugging Face Transformers** API.

---

## 🧹 Предобработка данных

После преобразования аннотаций данные в `train.csv` выглядят так:

```
sample;annotation
aa;['O']
aala;['O']
aarcca;['O']
abon;['O']
abso;['BRAND']
abtoys игруш;['BRAND', 'TYPE']
abtoys игрушки;['BRAND', 'TYPE']
actimel;['BRAND']
afanasiy пиво;['BRAND', 'TYPE']
...
```

Таким образом, каждая строка представляет собой токенизированный текст и список сущностей без BIO-префиксов и индексов.  
Это позволило упростить задачу для модели и повысить устойчивость при обучении.

---

## 🧠 Архитектура и обучение модели

- **Базовая модель:** `AI-Forever/RuBERT-base`
- **Tokenizer:** `AutoTokenizer.from_pretrained("AI-Forever/RuBERT-base")`
- **Split:** 90% train / 10% validation
- **Batch size:**  
  - train: 8  
  - eval: 16  
- **Learning rate:** `5e-5`
- **Weight decay:** `0.01`
- **Max sequence length:** `64`
- **Epochs:** `3–5`
- **Save best models:** каждые 200 шагов сохраняются лучшие 5 по Macro F1
- **FP16:** `True` (ускорение на GPU T4)
- **Seed:** `42`
- **Metric:** `Macro F1` через `seqeval`

Дополнительно использовалась функция дообучения — **fine-tune на одной эпохе** с пониженным learning rate после выбора лучшей модели из топ-5.

---

## 🧩 Ключевые модули

### 🧾 `preprocess.py`
Отвечает за:
- загрузку датасета;
- разбор аннотаций;
- создание токенов и меток;
- сбор token-label пар для HuggingFace `Dataset`.

### 🧠 `train.py`
- Загружает данные;
- Создаёт train/val split;
- Токенизирует с выравниванием лейблов;
- Обучает `AutoModelForTokenClassification`;
- Сохраняет лучшую модель;
- Автоматически запускает инференс после обучения.

### 🔮 `predict.py`
- Загружает сохранённую модель;
- Прогоняет все образцы из `submission.csv`;
- Добавляет BIO-префиксы и индексы;
- Возвращает результат в требуемом для ответа сервера формате.

---

## 📈 Результаты

| Модель | Macro F1 | Особенности |
|--------|-----------|-------------|
| DeepPavlov/rubert-base-cased | 0.81 | без предобработки, исходные данные |
| AI-Forever/RuBERT-base | **0.94–0.95** | с предобработкой и fine-tune |

---

## 🚀 Запуск проекта

### 🔹 Установка

```bash
git clone https://github.com/SelfDeprecation/NER-X5.git
cd NER-X5
python -m venv .venv
source .venv/bin/activate    # для Linux/Mac
.venv\Scripts\activate       # для Windows

pip install -r requirements.txt
```

---

### 🔹 Подготовка данных
Перед обучением необходимо запустить **предобработку**:  

```bash
python preprocess.py
```

После выполнения появятся очищенные файлы `train_cleaned.csv` и `submission_cleaned.csv`.  

---

### 🔹 Основное обучение модели

Запуск стандартного обучения (при желании можно указывать другие параметры вручную):

```bash
python src/train.py --train_csv data/train_cleaned.csv
```

---

### 🔹 Дообучение сохранённой модели (fine-tuning)

Для «дотюнинга» уже сохранённой модели на одну эпоху используется отдельный скрипт (при желании можно указывать другие параметры вручную):

```bash
python src_special/train_special.py --train_csv data/train_cleaned.csv
```

---

### 🔹 Предсказания

Для генерации предсказаний необходимо загрузить модель с помощью функции `load_model` в `predict.py` и вызвать функцию `predict_sample` для генерации предсказания.

---

### 🔹 Метрики и сохранение лучших моделей

В процессе обучения считается **macro F1 score** по валидационной выборке.  
Добавлен специальный колбэк, который:
- каждые 200 шагов считает F1;  
- сохраняет модель **только если F1 улучшился**;  
- хранит **топ-5 моделей** с наибольшим F1;  
- старые чекпоинты автоматически удаляются;
- дообучает при необходимости с такой же механикой, но немного другими параметрами.

Таким образом, после обучения у вас всегда есть папка с лучшими моделями (`outputs/run/*`), среди которых можно выбрать финальную.

---

## ⚡ Оптимизация и доработка

- Использовались оптимизации на уровне FP16 для ускорения обучения на GPU.
- Пришлось вручную добавлять BIO-префиксы после предсказаний — модель изначально обучалась только на самих сущностях.
- Для выбора лучшей модели проводился оффлайн-оценочный прогон по validation и финальная ручная дообучка с уменьшенным learning rate.

---

## 🧩 Технические сложности

- **BIO-схема:** несколько раз ломалась токенизация, появлялись невалидные лейблы вроде `"B-O"`. Проблема решена добавлением явной проверки.
- **Оптимизация скорости инференса:** пришлось адаптировать код под серверные ограничения (время отклика < 1 сек).  
  Использовались batched-предсказания и кеширование токенайзера.
- **Ручное восстановление BIO после инференса:** ответ финального `predict` приводился к нужному серверному формату.

---

## 💬 Истории и инсайты

> “Я несколько дней бился с тем, чтобы выйти за 81 % F1, но не получалось.  
> После того как добавил предобработку и сменил модель, кривая пошла вверх сразу.  
> Это был тот момент, когда проект реально ожил.”

Команда отмечает, что самым сложным этапом была борьба за стабильность и скорость.  
Серверная часть требовала ответа менее чем за секунду — пришлось оптимизировать всё, от токенизации до батчирования GPU-вызовов.  

---

## API модуль

### Основные технологии:
- **FastAPI** - веб-фреймворк для создания API
- **Uvicorn** - ASGI сервер для запуска FastAPI приложения
- **Pydantic** - валидация данных и сериализация

### Архитектура
1. Класс ModelManager - управляет загрузкой и использованием обученной модели
2. FastAPI приложение - предоставляет REST API эндпоинты
3. Входные и выходные данные валидируются через Pydantic

### Процесс обработки запроса:
1. Клиент отправляет POST запрос на /api/predict с текстом для анализа
2. Сервис загружает модель (при первом запросе) или использует уже загруженную
3. Текст токенизируется и передается в модель
4. Модель возвращает предсказания для каждого токена
5. Результаты преобразуются в формат BIO (Begin-Inside-Outside)
6. Возвращается список сущностей с их позициями в тексте

### API Эндпоинты
#### 1. Проверка работоспособности  
```GET /```  
Возвращает статус сервиса.  
Ответ:  
```json
{
  "status": "ok"
}
```
#### 2. Health Check  
```GET /health-check```
Проверка здоровья сервиса  
Ответ:  
```json
{
  "status": "ok"
}
```
#### 3. Предсказание сущностей  
```POST /api/predict```  
Тело запроса:  
```json
{
  "input": "{abtoys игрушки}"
}
```
Ответ:  
```
[
    {
        "start_index": 0,
        "end_index": 6,
        "entity": "B-BRAND"
    },
    {
        "start_index": 7,
        "end_index": 13,
        "entity": "B-TYPE"
    }
]
```

### Логирование
Сервис ведет логирование с уровнем INFO, включая:
- Время обработки каждого запроса
- Входной текст и результат предсказания

### Производительность

- Модель загружается один раз при первом запросе (ленивая загрузка)
- Поддерживается GPU ускорение при наличии CUDA
- Асинхронная обработка запросов через FastAPI
- Замер времени обработки для мониторинга производительности

### Docker

Проект включает Dockerfile для контейнеризации приложения. Docker образ включает:
- Python 3.9 slim базовый образ
- Все необходимые зависимости
- Настроенную рабочую среду
- Безопасного пользователя для запуска

### Запуск сервера:
**Предварительные требования:**
1. Убедитесь, что обученная модель находится в папке `outputs/run/`
2. Установите все зависимости из `requirements.txt`

**Запуск из корневой папки проекта:**
```bash
python main.py
```
**Прямой запуск через uvicorn:**
```bash
uvicorn api.service:app --host 0.0.0.0 --port 8000
```
**Запуск через Docker:**
```bash
# Сборка Docker образа
docker build -t ner-x5-api .

# Запуск контейнера
docker run -p 8000:8000 ner-x5-api
```

## 📦 Пример итогового формата данных

**Вход (запрос к серверу):**

```json
{"input": "{abtoys игрушки}"}
```

**Выход (ответ сервера):**

```json
[
    {"start_index": 0, "end_index": 6, "entity": "B-BRAND"},
    {"start_index": 7, "end_index": 13, "entity": "B-TYPE"},
]
```

---

## 🧭 Итог

Этот проект прошёл путь от базового DeepPavlov до мощной RuBERT-архитектуры, от сырых аннотаций — к аккуратно выстроенной BIO-схеме, и показал, как правильно выстроенная предобработка и fine-tuning могут поднять метрику почти на **15 %**.
